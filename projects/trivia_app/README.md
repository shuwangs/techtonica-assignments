

# 🧪 TESTING

## Test Suite
### Unite Tests Located in `server/units/__test__/`
- *AnalyzeQuizResults.test.js*: Validates the scoring algorithm, including edge case handling for mismatched questions, whitespace in answers, and missing data.
- 
```bash
cd server
npm test
```

